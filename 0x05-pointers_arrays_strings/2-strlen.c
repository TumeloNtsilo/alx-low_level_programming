#include "main.h"
#include <stdio.h>

/**
 * _strlen - Returns the length of a string
 * @Str: String to get the length of
 * Return: Length of the string
 *
 */

int _strlen(char *s)
{

	size_t length = 0;

	while (*str++)
	length++;
	return (length);
}
